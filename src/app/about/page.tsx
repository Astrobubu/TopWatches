import { AboutContent } from "@/components/about/about-content"

export const metadata = {
  title: "About | Golden Planet Watches",
  description:
    "Learn about Golden Planet Watches — buy and sell luxury watches with confidence",
}

export default function AboutPage() {
  return <AboutContent />
}
